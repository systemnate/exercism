class Acronym
  VERSION = 1
  def self.abbreviate(given)
    given.split.map do |w|
      if w == w.upcase
        w
      else
        w.split(/(?=[A-Z])/)
      end
    end.flatten.map { |s| s[0].capitalize }.join
  end
end
